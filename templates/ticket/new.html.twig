{% block body %}
    <h1>{{ 'theme.create-ticket'|trans }}</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ 'theme.submit'|trans }}</button>
    {{ form_end(form) }}
{% endblock %}