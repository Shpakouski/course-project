<form method="post" action="{{ path('app_collection_delete', {'user': user.id,'collection': collection.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ collection.id) }}">
    <button class="btn btn-primary">Delete</button>
</form>
