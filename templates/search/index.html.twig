{% extends 'base.html.twig' %}

{% block title %}Serch!{% endblock %}

{% block body %}

    <div class="container mt-5">
            <h2>{{ 'theme.search-results'|trans }}✅</h2>
            <ul class="list-group">
                {% for item in results %}
                    <li class="list-group-item">
                        <a href="#">{{ item.name }}</a>
                    </li>
                {% else %}
                    <li class="list-group-item">{{ 'theme.no-results-found'|trans }}</li>
                {% endfor %}
            </ul>
    </div>
{% endblock %}